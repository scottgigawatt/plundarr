#
# Copyright 2025-2026 Scott Gigawatt
#
# Licensed under the Apache License, Version 2.0.
#
# .pre-commit-config.yaml: Repository lint, secret, workflow, shell, policy,
#                          and YAML validation hooks.
#

repos:
  #
  # Scan changed content for hardcoded secrets before it can be committed.
  #
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks

  #
  # Compare detected secrets against the reviewed repository baseline.
  #
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        args: ["--baseline", ".secrets.baseline"]
        exclude: package.lock.json

  #
  # Run general file hygiene, syntax, shebang, and whitespace checks while
  # leaving generated service templates to their specialized validators.
  #
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-added-large-files
      - id: check-executables-have-shebangs
      - id: check-merge-conflict
      - id: check-shebang-scripts-are-executable
      - id: check-toml
      - id: check-yaml
        # MkDocs validates its trusted Python-tagged Mermaid formatter itself.
        exclude: ^(?:mkdocs\.yml|docker/services/(?:homepage/config/.*|[^/]+/compose\.yml))$
      - id: end-of-file-fixer
      - id: trailing-whitespace
        args: ["--markdown-linebreak-ext=md"]

  #
  # Validate every Markdown document with the shared repository configuration.
  #
  - repo: https://github.com/DavidAnson/markdownlint-cli2
    rev: v0.23.2
    hooks:
      - id: markdownlint-cli2
        pass_filenames: false

  #
  # Enforce strict two-space YAML outside generated service template directories.
  #
  - repo: https://github.com/adrienverge/yamllint
    rev: v1.38.0
    hooks:
      - id: yamllint
        args:
          - "--strict"
          - "-d"
          - "{extends: default, rules: {document-start: disable, indentation: {spaces: 2, indent-sequences: true}, line-length: disable, truthy: disable}}"
        exclude: ^docker/services/(?:homepage/config/|[^/]+/compose\.yml$)

  #
  # Validate GitHub Actions expressions, permissions, and shell fragments.
  #
  - repo: https://github.com/rhysd/actionlint
    rev: v1.7.12
    hooks:
      - id: actionlint

  #
  # Check project-owned shell scripts for correctness and portability issues.
  #
  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.11.0.1
    hooks:
      - id: shellcheck

  #
  # Enforce synchronized build pins and the shared image-tag channel policy.
  #
  - repo: local
    hooks:
      - id: check-build-pin-policy
        name: check build pin policy
        entry: test/policy/check-build-pin-policy.sh
        language: script
        pass_filenames: false

      - id: check-image-tag-policy
        name: check image tag policy
        entry: test/policy/check-image-tag-policy.sh
        language: script
        pass_filenames: false
