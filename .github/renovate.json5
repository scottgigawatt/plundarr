//
// Copyright 2025-2026 Scott Gigawatt
//
// Licensed under the Apache License, Version 2.0.
//
// renovate.json5: Automated dependency update policy for Plundarr.
//
{
    //
    // Renovate schema and shared safe defaults.
    //
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "extends": [
        "config:recommended",
        "helpers:pinGitHubActionDigests",
        ":enablePreCommit"
    ],

    // Keep dependency ignores while allowing the real test/typing/Dockerfile.
    "ignorePaths": [
        "**/node_modules/**",
        "**/bower_components/**",
        "**/vendor/**",
        "**/examples/**",
        "**/__tests__/**",
        "**/tests/**",
        "**/__fixtures__/**"
    ],

    //
    // Keep dependency work predictable for a single-maintainer repository.
    //
    "timezone": "America/New_York",
    "schedule": ["before 8am"],
    "dependencyDashboard": true,

    // Merge dependency PRs after required checks pass; keep branches current.
    "automerge": true,
    "automergeType": "pr",
    "automergeStrategy": "squash",
    "platformAutomerge": true,
    "rebaseWhen": "behind-base-branch",
    "labels": ["dependencies"],
    "assignees": ["scottgigawatt"],
    "reviewers": ["scottgigawatt"],

    //
    // Renovate owns updates that Dependabot previously duplicated.
    //
    "enabledManagers": [
        "custom.regex",
        "docker-compose",
        "dockerfile",
        "github-actions",
        "npm",
        "pep621",
        "pip_requirements",
        "pre-commit"
    ],

    //
    // Track the docs Python version, test tools, and shared image pins.
    // Keep direct and shell-default forms separate so each pin is extracted once.
    //
    "customManagers": [
        {
            "customType": "regex",
            "managerFilePatterns": [
                "/^Makefile$/"
            ],
            "matchStrings": [
                "DOCS_PYTHON_VERSION\\s+\\?=\\s*(?<currentValue>\\d+\\.\\d+\\.\\d+)"
            ],
            "depNameTemplate": "python",
            "datasourceTemplate": "python-version",
            "versioningTemplate": "python"
        },
        {
            "customType": "regex",
            "managerFilePatterns": [
                "/^docker/Dockerfile$/",
                "/^test/typing/Dockerfile$/",
                "/^\\.github/workflows/build-and-push\\.yml$/"
            ],
            "matchStrings": [
                "ALPINE_TAG[=:]\\s*(?<currentValue>[^@\\s]+)@(?<currentDigest>sha256:[a-f0-9]+)"
            ],
            "depNameTemplate": "alpine",
            "datasourceTemplate": "docker",
            "versioningTemplate": "docker"
        },
        {
            "customType": "regex",
            "managerFilePatterns": [
                "/^example\\.maraudarr\\.env$/"
            ],
            "matchStrings": [
                "ALPINE_TAG=\"\\$\\{ALPINE_TAG:-(?<currentValue>[^@\\s}]+)@(?<currentDigest>sha256:[a-f0-9]+)\\}\""
            ],
            "depNameTemplate": "alpine",
            "datasourceTemplate": "docker",
            "versioningTemplate": "docker"
        },
        {
            "customType": "regex",
            "managerFilePatterns": [
                "/^docker/Dockerfile$/",
                "/^\\.github/workflows/build-and-push\\.yml$/"
            ],
            "matchStrings": [
                "DOCKER_COMPOSE_TAG[=:]\\s*(?<currentValue>[^@\\s]+)@(?<currentDigest>sha256:[a-f0-9]+)"
            ],
            "depNameTemplate": "docker/compose-bin",
            "datasourceTemplate": "docker",
            "versioningTemplate": "docker"
        },
        {
            "customType": "regex",
            "managerFilePatterns": [
                "/^example\\.maraudarr\\.env$/"
            ],
            "matchStrings": [
                "DOCKER_COMPOSE_TAG=\"\\$\\{DOCKER_COMPOSE_TAG:-(?<currentValue>[^@\\s}]+)@(?<currentDigest>sha256:[a-f0-9]+)\\}\""
            ],
            "depNameTemplate": "docker/compose-bin",
            "datasourceTemplate": "docker",
            "versioningTemplate": "docker"
        }
    ],

    //
    // Keep related update types grouped and dependency pins deterministic.
    //
    "packageRules": [
        {
            "groupName": "Python runtime",
            "matchDepNames": ["python"]
        },
        {
            "matchDatasources": ["docker"],
            "pinDigests": true
        },
        {
            "groupName": "GitHub Actions",
            "matchManagers": ["github-actions"]
        },
        {
            "groupName": "pre-commit hooks",
            "matchManagers": ["pre-commit"]
        },
        {
            "groupName": "Docker images",
            "matchDatasources": ["docker"]
        },
        {
            "description": "Keep Python dependencies exact and hash-verified",
            "matchManagers": ["pep621", "pip_requirements"],
            "rangeStrategy": "pin"
        }
    ]
}
