# Speedtest Tracker configuration 📡

Speedtest Tracker stores its database, results, schedules, and application settings in this directory. Set the generated application key and database values in `.env` before first launch, then configure schedules and notifications through the Web UI.

Results are durable application state, so include this directory in deployment backups.
