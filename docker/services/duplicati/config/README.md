# 🔄 Duplicati Configuration

Duplicati stores its database and backup settings here. Maraudarr writes the
required encryption key and web password to the root `.env` file during
generation; keep that file backed up and private.

Configure backup destinations and schedules in Duplicati's web interface.
