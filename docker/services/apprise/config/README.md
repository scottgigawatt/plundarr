# 🔔 Apprise Configuration

Maraudarr creates this directory so every selected service has an obvious home. The default Apprise API configuration is stateless and uses environment values, so no persistent files are required here unless ye intentionally enable them.
