# Notifiarr configuration 🔔

Notifiarr creates its client configuration in this directory. Complete setup through the Web UI and keep the resulting API keys and application credentials inside generated deployment state.
