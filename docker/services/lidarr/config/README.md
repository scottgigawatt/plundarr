# Lidarr configuration 🎵

Lidarr creates its database and application settings here on first launch. Maraudarr preserves those files when the stack is regenerated.

After launch, connect Lidarr to Prowlarr and a selected download client, then use `/music` as its root folder.
