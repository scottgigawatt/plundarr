#
# NZBGet environment variables
#
NZBGET_TAG="${NZBGET_TAG:-latest}"
NZBGET_WEBUI_PORT="${NZBGET_WEBUI_PORT:-6789}"  # Change only for a host-port conflict
NZBGET_CONFIG_PATH="${NZBGET_CONFIG_PATH:-./config/nzbget}"
NZBGET_USER="${NZBGET_USER:-nzbget}"
NZBGET_PASS="${NZBGET_PASS:-changeme}"
