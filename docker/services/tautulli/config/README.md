# Tautulli Configuration 📈

Tautulli stores its database, `config.ini`, logs, backups, and exported data in
this directory. Protect it as durable application state and keep its API key
out of source control.
