# ImageMaid configuration 🖼️

ImageMaid stores its `.env`, database, cache, and logs in this directory. Begin with report-oriented settings and review the output before enabling destructive cleanup options.

Plex credentials belong in the generated private deployment state, never in the Plundarr repository.
