#
# Plex Media Server environment variables
#
PLEX_TAG="${PLEX_TAG:-latest}"
PLEX_VERSION="${PLEX_VERSION:-docker}"
PLEX_CLAIM="${PLEX_CLAIM:-}"
PLEX_CONFIG_PATH="${PLEX_CONFIG_PATH:-./config/plex}"
PLEX_TRANSCODE_PATH="${PLEX_TRANSCODE_PATH:-./config/plex/transcode}"
