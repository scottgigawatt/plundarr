# FlareSolverr configuration 🌩️

FlareSolverr is configured primarily through environment values and does not require operator-managed files for a standard Plundarr deployment. This directory exists to keep generated service structure predictable and to receive any application state created by future image versions.

Add FlareSolverr to a supported indexer only when that indexer requires it; the upstream project does not solve every challenge provider.
