# Kometa Overlay Reset state ⚠️

Overlay Reset may write logs and automatic resume state here. Plex credentials and tool options remain in the generated deployment's private top-level `.env`.

The service is profile-gated, defaults to a dry run, and must be invoked through the explicit one-shot Make target.
