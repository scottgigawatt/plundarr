# 🔭 Watchtower Configuration

Watchtower is configured through environment values and the Docker socket, so the selected service does not require persistent files. This directory exists to keep generated service structure predictable.
