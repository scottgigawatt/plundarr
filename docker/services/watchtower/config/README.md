# 🔭 Watchtower Configuration

Watchtower is configured through environment values and the Docker socket, so
its default Plundarr service does not require persistent files. This directory
exists to keep generated service structure predictable.
